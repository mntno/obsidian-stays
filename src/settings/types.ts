export interface PluginSettings {
	/** The folder in which person notes are created. Trimmed. */
	peopleFolder: string;
	/** The folder in which booking notes are created. Trimmed. */
	bookingsFolder: string;
}
